# GitHub Webhook サーバー設定
# 本番環境用の環境変数設定

# GitHub Webhook 設定
WEBHOOK_SECRET=your-secret-key-here
PROJECT_PATH=/path/to/your/project
DEPLOY_COMMAND=git pull && npm install && pm2 restart app
ALLOWED_BRANCHES=main,master

# サーバー設定
PORT=3000
NODE_ENV=production

# ログ設定
ENABLE_DETAILED_LOGS=false

# 設定例:
# WEBHOOK_SECRET=abcd1234567890efgh
# PROJECT_PATH=/var/www/my-app
# DEPLOY_COMMAND=cd /var/www/my-app && git pull && yarn install && yarn build && pm2 restart my-app
# ALLOWED_BRANCHES=main,master,develop